// Approval.cpp
#include "Approval.h"

// TEMP: auto-approve for API mode
// Phase 9.3 → OTP / supervisor approval API

bool requestApproval(
    ApprovalType type,
    float amount,
    const std::string& reason
) {
    return true;
}
